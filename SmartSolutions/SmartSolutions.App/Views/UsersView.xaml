<UserControl x:Class="SmartSolutions.App.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes">

    <md:DialogHost IsOpen="{Binding IsDialogOpen, Mode=OneWay}" CloseOnClickAway="False">
        <md:DialogHost.DialogContent>
            <StackPanel Margin="24" MinWidth="300">
                <!-- Add User form -->
                <StackPanel Visibility="{Binding IsAddDialogOpen, Converter={StaticResource BoolToVisibility}}">
                    <TextBlock Text="Add User"
                               Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,16"/>
                    <TextBox md:HintAssist.Hint="Username *"
                             Text="{Binding NewUsername, UpdateSourceTrigger=PropertyChanged}"
                             Margin="0,0,0,8"/>
                    <PasswordBox md:HintAssist.Hint="PIN *" x:Name="AddPinBox" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding ErrorMessage}"
                               Foreground="{DynamicResource MaterialDesignValidationErrorBrush}"
                               MinHeight="20" TextWrapping="Wrap"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Content="Add User" Click="OnConfirmAddUserClicked"
                                Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,8,0"/>
                        <Button Content="Cancel" Command="{Binding CancelAddDialogCommand}"
                                Style="{StaticResource MaterialDesignOutlinedButton}"/>
                    </StackPanel>
                </StackPanel>

                <!-- Reset PIN form -->
                <StackPanel Visibility="{Binding IsResetPinDialogOpen, Converter={StaticResource BoolToVisibility}}">
                    <TextBlock Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Margin="0,0,0,16">
                        <TextBlock.Text>
                            <MultiBinding StringFormat="Reset PIN for '{0}'">
                                <Binding Path="ResetPinTargetUser.Username"/>
                            </MultiBinding>
                        </TextBlock.Text>
                    </TextBlock>
                    <PasswordBox md:HintAssist.Hint="New PIN *" x:Name="ResetPinBox" Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding ErrorMessage}"
                               Foreground="{DynamicResource MaterialDesignValidationErrorBrush}"
                               MinHeight="20" TextWrapping="Wrap"/>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Content="Reset PIN" Click="OnConfirmResetPinClicked"
                                Style="{StaticResource MaterialDesignRaisedButton}" Margin="0,0,8,0"/>
                        <Button Content="Cancel" Command="{Binding CancelResetPinDialogCommand}"
                                Style="{StaticResource MaterialDesignOutlinedButton}"/>
                    </StackPanel>
                </StackPanel>
            </StackPanel>
        </md:DialogHost.DialogContent>

        <Grid Margin="24">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="Users" Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                               VerticalAlignment="Center" Margin="0,0,16,0"/>
                    <Button Content="+ Add User" Command="{Binding OpenAddDialogCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"/>
                </StackPanel>
                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="{DynamicResource MaterialDesignValidationErrorBrush}"
                           MinHeight="20" TextWrapping="Wrap"/>
            </StackPanel>
            <DataGrid Grid.Row="1" ItemsSource="{Binding Users}"
                      AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                    <DataGridTextColumn Header="Status"
                                        Binding="{Binding IsActive, Converter={StaticResource BoolToActiveConverter}}"
                                        Width="100"/>
                    <DataGridTemplateColumn Width="220">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Reset PIN"
                                            Command="{Binding DataContext.OpenResetPinDialogCommand,
                                                      RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource MaterialDesignFlatButton}" Padding="4,0"/>
                                    <Button Command="{Binding DataContext.ToggleActiveCommand,
                                                     RelativeSource={RelativeSource AncestorType=UserControl}}"
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource MaterialDesignFlatButton}" Padding="4,0">
                                        <Button.Content>
                                            <TextBlock>
                                                <TextBlock.Style>
                                                    <Style TargetType="TextBlock">
                                                        <Setter Property="Text" Value="Deactivate"/>
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                                <Setter Property="Text" Value="Reactivate"/>
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </TextBlock.Style>
                                            </TextBlock>
                                        </Button.Content>
                                    </Button>
                                </StackPanel>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>
    </md:DialogHost>
</UserControl>
